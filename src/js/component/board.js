import React from "react";

const BoardGame = () => {
	return (
		<h2>Armando el tablero</h2>
		// let board_x = [1, 2, 3, 4, 5, 6, 7, 8, 9],
		// let board_y = [1, 2, 3, 4, 5, 6, 7, 8, 9],
		// console.log(board_x)

		// 0 = empty space
		// 1 = the ship´s place
		// 2 = fire on the ship
		// 3 = shoot in the water
	);
};

export default BoardGame;
